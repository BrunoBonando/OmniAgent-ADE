# Sample Project

A tiny golden fixture used by `brain-ingest`'s tests. Uses the auth helper
in `src/auth.ts`, which imports its password hashing from `src/util.ts`.

See [notes](docs/notes.md) for internal notes.
