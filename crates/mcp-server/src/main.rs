// Phase 3 fills this in: the frozen MCP tool contract (search_brain, get_context,
// related, record_decision, record_note, list_projects) over stdio.
fn main() {
    println!("omniagent-mcp: not yet implemented (Phase 3)");
}
